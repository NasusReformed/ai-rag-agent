# Copy this to .env and configure with your values
# Never commit .env to git

# Database URL (PostgreSQL with pgvector)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Hugging Face API (get token from https://huggingface.co/settings/tokens)
# HF_API_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxx
# HF_MODEL=meta-llama/Llama-3.2-3B-Instruct

# Embeddings model
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# RAG configuration
# RAG_TOP_K=4

# LLM provider (hf or ollama)
# LLM_PROVIDER=hf

# Ollama configuration (if using local LLM)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2
